name: attention_terminal
version: "1.0.0"
config-version: 2

profile: attention_terminal

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  attention_terminal:
    staging:
      +materialized: view
    marts:
      core:
        +materialized: view
      search:
        +materialized: view
