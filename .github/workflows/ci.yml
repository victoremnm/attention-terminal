name: CI

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  next:
    name: Next.js build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Use Node.js 24.x
        uses: actions/setup-node@v7
        with:
          node-version: "24.x"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build

  migrations:
    name: Goose migrations
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Use Go 1.24.x
        uses: actions/setup-go@v7
        with:
          go-version: "1.24.x"

      - name: Install Goose
        run: go install github.com/pressly/goose/v3/cmd/goose@v3.25.0

      - name: Validate Goose migrations
        run: goose -dir migrations validate

      - name: Apply and roll back migrations in OSS ClickHouse
        run: ./scripts/test-migrations-local.sh
        env:
          RUN_MIGRATION_ROLLBACK: "1"

  tests:
    name: Query integration tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Use Go 1.24.x
        uses: actions/setup-go@v7
        with:
          go-version: "1.24.x"
      - uses: actions/setup-node@v7
        with:
          node-version: "24.x"
          cache: npm
      - run: npm ci
      - name: Install Goose
        run: go install github.com/pressly/goose/v3/cmd/goose@v3.25.0
      - name: Apply ClickHouse migrations
        run: ./scripts/migrate.sh up
        env:
          CLICKHOUSE_HOST: ${{ secrets.CLICKHOUSE_HOST }}
          CLICKHOUSE_USER: ${{ secrets.CLICKHOUSE_USER }}
          CLICKHOUSE_PASSWORD: ${{ secrets.CLICKHOUSE_PASSWORD }}
          CLICKHOUSE_DATABASE: ${{ secrets.CLICKHOUSE_DATABASE }}
      - name: Run query tests
        run: npm test
        env:
          CLICKHOUSE_URL: ${{ secrets.CLICKHOUSE_URL }}
          CLICKHOUSE_USER: ${{ secrets.CLICKHOUSE_USER }}
          CLICKHOUSE_PASSWORD: ${{ secrets.CLICKHOUSE_PASSWORD }}
          CLICKHOUSE_DATABASE: ${{ secrets.CLICKHOUSE_DATABASE }}
